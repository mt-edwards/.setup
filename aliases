# virtual environments
alias activate='source .venv/bin/activate'
alias pipup='pip install --upgrade pip'
alias pipml='pip install -r ~/.setup/requirements.txt'

# jupyter notebook
alias jn='jupyter notebook'
